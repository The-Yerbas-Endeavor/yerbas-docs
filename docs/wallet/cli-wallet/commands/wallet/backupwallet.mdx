---
sidebar_position: 4
---

import ReactPlayer from 'react-player';

# backupwallet

_backupwallet "destination"_

Safely copies current wallet file to destination, which can be a directory or a path with filename.

`yerbas-cli backupwallet "backup.dat"`

<ReactPlayer
  playing
  controls
  url={[
    {
      src: `${require('./assets/backupwallet.webm').default}`,
      type: 'video/webm',
    },
  ]}
/>

## Arguments

| Argument      | Type     | Description                       |
| ------------- | -------- | --------------------------------- |
| "destination" | (string) | The destination directory or file |

```bash
Arguments:
1. "destination"   (string) The destination directory or file
```

## Output

| Result | Type | Description |
| ------ | ---- | ----------- |
| ...    | ...  | ...         |

```bash
Result:
{
  ...
}
```

## Examples

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "backupwallet", "params": ["backup.dat"] }' -H 'content-type: text/plain;' http://127.0.0.1:9998/
```
