---
sidebar_position: 37
---

import ReactPlayer from 'react-player';

# listwallets

_listwallets_

Returns a list of currently loaded wallets.
For full information on the wallet, use "getwalletinfo"

`yerbas-cli listwallets`

<ReactPlayer
  playing
  controls
  url={[
    {
      src: `${require('./assets/listwallets.webm').default}`,
      type: 'video/webm',
    },
  ]}
/>

## Output

| Result       | Type     | Description              |
| ------------ | -------- | ------------------------ |
| "walletname" | (string) | (string) the wallet name |

```bash
Result:
[                         (json array of strings)
  "walletname"            (string) the wallet name
   ...
]
```

## Examples

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "listwallets", "params": [] }' -H 'content-type: text/plain;' http://127.0.0.1:9998/
```
