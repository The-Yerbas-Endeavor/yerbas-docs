---
sidebar_position: 7
---

import ReactPlayer from 'react-player';

# privatesend

_privatesend "command"_

```bash
# Available commands:
  start       - Start mixing
  stop        - Stop mixing
  reset       - Reset mixing
```

`yerbas-cli privatesend start`

<ReactPlayer
  playing
  controls
  url={[
    {
      src: `${require('./assets/privatesend.webm').default}`,
      type: 'video/webm',
    },
  ]}
/>

## Arguments

| Argument  | Type     | Description            |
| --------- | -------- | ---------------------- |
| "command" | (string) | The command to execute |

```bash
Arguments:
1. "command"        (string or set of strings, required) The command to execute
```

## Output

| Result | Type | Description |
| ------ | ---- | ----------- |
| ...    | ...  | ...         |

```bash
Result:
{
}
```

## Examples

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "privatesend", "params": ["start"] }' -H 'content-type: text/plain;' http://127.0.0.1:9998/
```
