---
sidebar_position: 11
---

import ReactPlayer from 'react-player';

# voteraw

_voteraw mn-collateral-tx-hash mn-collateral-tx-index governance-hash vote-signal yes|no|abstain time vote-sig_

Compile and relay a governance vote with provided external signature instead of signing vote internally

<!-- <ReactPlayer playing controls url={[
    {src: `${require('./assets/placeholder.webm').default}`, type: 'video/webm'}]} /> -->

## Output

| Result | Type | Description |
| ------ | ---- | ----------- |
| ...    | ...  | ...         |

```bash
Result:
{
  ...
}
```

## Examples

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "voteraw", "params": [] }' -H 'content-type: text/plain;' http://127.0.0.1:9998/
```
