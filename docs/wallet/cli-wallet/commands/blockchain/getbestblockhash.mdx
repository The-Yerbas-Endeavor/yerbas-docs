---
sidebar_position: 1
---

import ReactPlayer from 'react-player';

# getbestblockhash

Returns the hash of the best (tip) block in the longest blockchain.

`yerbas-cli getbestblockhash`

<ReactPlayer
  playing
  controls
  url={[
    {
      src: `${require('./assets/getbestblockhash.webm').default}`,
      type: 'video/webm',
    },
  ]}
/>

## Output

| Result | Type     | Description                |
| ------ | -------- | -------------------------- |
| "hex"  | (string) | The block hash hex encoded |

```bash
Result:
"hex"      (string) the block hash hex encoded
```

## Examples

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "getbestblockhash", "params": [] }' -H 'content-type: text/plain;' http://127.0.0.1:9998/
```
