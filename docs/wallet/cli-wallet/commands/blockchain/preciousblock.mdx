---
sidebar_position: 22
---

import ReactPlayer from 'react-player';

# preciousblock

_preciousblock "blockhash"_

Treats a block as if it were received before others with the same work.

A later preciousblock call can override the effect of an earlier one.

The effects of preciousblock are not retained across restarts.

`yerbas-cli preciousblock "blockhash"`

<!-- <ReactPlayer playing controls url={[
    {src: `${require('./assets/placeholder.webm').default}`, type: 'video/webm'}]} /> -->

## Arguments

| Argument    | Type     | Description                               |
| ----------- | -------- | ----------------------------------------- |
| "blockhash" | (string) | the hash of the block to mark as precious |

```bash
Arguments:
1. "blockhash"   (string, required) the hash of the block to mark as precious
```

## Output

| Result | Type  | Description |
| ------ | ----- | ----------- |
| ...    | (...) | ...         |

```bash
Result:
{
  ...
}
```

## Examples

```bash
curl --user myusername --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "preciousblock", "params": ["blockhash"] }' -H 'content-type: text/plain;' http://127.0.0.1:9998/
```
